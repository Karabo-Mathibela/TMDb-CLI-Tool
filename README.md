# TMDb-CLI-Tool
This will be a simple command-line interface program that consumes the The Movie Database API and prints the data in a structured format, based on a series of subcommands provided by the user.

The program is currently in development.
